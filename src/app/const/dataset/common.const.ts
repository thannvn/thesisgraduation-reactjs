export enum DatasetVisibility {
  /*Create dataset */
  PUBLIC_DATASET = 'public',

  /*Update dataset */
  PRIVATE_DATASET = 'private'
}

export enum FILE_STATUS {
  ADD = 0,
  MODIFIED = 1,
  DELETE = 2,
};