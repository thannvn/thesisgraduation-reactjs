export enum DatasetVisibility {
  /*Create dataset */
  PUBLIC_DATASET = 1,

  /*Update dataset */
  PRIVATE_DATASET = 0
}

export enum FILE_STATUS {
  ADD = 0,
  MODIFIED = 1,
  DELETE = 2,
};

export enum IMAGE_TYPE {
  BANNER = 0,
  THUMBNAIL = 1,
}

export enum AccountMode {
  /*Create dataset */
  PUBLIC_ACCOUNT = 1,

  /*Update dataset */
  PRIVATE_ACCOUNT = 0
}